export enum Stacks {
  PUBLIC = 'STACK_PUBLIC',
  PRIVATE = 'STACK_PRIVATE',
  HOME = 'STACK_HOME',
}

export enum Routes {
  LOGIN = 'LOGIN',
  HOME = 'HOME',
  QUEUES = 'QUEUES',
}

export default Routes;
